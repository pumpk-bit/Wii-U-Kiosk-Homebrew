# Auto detect text files and perform LF normalization
* text=auto

# Bash / Python / CMake must stay LF so MSYS2 / Linux can run them after a Windows clone.
*.sh text eol=lf
*.py text eol=lf
*.cmake text eol=lf
